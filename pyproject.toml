[project]
name = "supportagent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pypdf", "openai", "psycopg[binary]", "pgvector", "fastapi", "uvicorn"]

[project.optional-dependencies]
dev = ["pytest"]
